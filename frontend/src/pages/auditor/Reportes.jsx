/**
 * @fileoverview Reportes — Reportes financieros (Auditor, solo lectura).
 * Página stub — pendiente de implementación completa.
 */
export default function AuditorReportes() {
  return (
    <div className="erp-page">
      <div className="erp-eyebrow">Auditoría · Reportes</div>
      <h1 className="erp-title">Reportes.</h1>
      <p className="erp-sub">Vista de solo lectura — en construcción.</p>
    </div>
  );
}
